name: Hello World Echo
on:
  pull_request:
    branches: [ "main" ]
jobs:
  echo_hello_on_push:
    runs-on: self-hosted 
    steps:
      - name: Echo Hello World on push
        run: echo "Hello world"
